# ACK 重试机制实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通 ACK 与重试链路：messageId 贯穿全流程，消费失败消息回传 Broker 进入重试队列，到期后回投原 Topic。

**Architecture:** 6 个文件改动，分 8 个 task。自底向上：Store 层 messageId 持久化 → Broker/Client 透传 → AckManager 回投回调 → BrokerRequestHandler 真正实现 ACK → Consumer 失败回传。

**Tech Stack:** Java 8, JUnit 5, Mockito

## Global Constraints

- Pull 线程 offset 推进逻辑不变
- `Consumer.java` 接口不变
- 协议消息类型不变（复用 `ACK_MESSAGE_REQUEST/RESPONSE`）
- AckManager 已有重试/死信/超时逻辑不变
- messageId 生成逻辑由 Producer 侧负责，本方案只透传

---

### Task 1: Store Message — 新增 messageId 字段

**Files:**
- Modify: `flare-mq-store/src/main/java/com/flare/mq/store/Message.java`

- [ ] **Step 1: 在 Message 类中添加 messageId 字段**

在 `storeSize` 字段（第 67 行）后添加：

```java
/**
 * 消息ID
 */
private String messageId;
```

- [ ] **Step 2: 添加 getter/setter**

在 `setStoreSize` 之后添加：

```java
public String getMessageId() {
    return messageId;
}

public void setMessageId(String messageId) {
    this.messageId = messageId;
}
```

- [ ] **Step 3: 编译验证**

Run: `mvn compile -pl flare-mq-store -am`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add flare-mq-store/src/main/java/com/flare/mq/store/Message.java
git commit -m "feat: add messageId field to store Message class"
```

---

### Task 2: MessageSerializer — messageId 序列化/反序列化

**Files:**
- Modify: `flare-mq-store/src/main/java/com/flare/mq/store/MessageSerializer.java`

**Interfaces:**
- Consumes: `Message.getMessageId()` (from Task 1)
- Produces: serialized message now contains messageId in properties

- [ ] **Step 1: 在序列化写入 messageId 到 Properties**

修改 `serialize()` 方法（第 32 行），在调用 `serializeProperties` 之前，将 messageId 写入 properties：

```java
// 将 messageId 写入 properties 以便序列化
Map<String, String> propsForSerialize = message.getProperties();
if (message.getMessageId() != null && !message.getMessageId().isEmpty()) {
    if (propsForSerialize == null) {
        propsForSerialize = new HashMap<>();
    }
    propsForSerialize.put("_MSG_ID_", message.getMessageId());
}
byte[] propertiesBytes = serializeProperties(propsForSerialize);
```

> 注意：用 properties 携带 messageId，无需修改 MessageSerializer 的二进制布局。key 用 `_MSG_ID_` 避免与业务属性冲突。

- [ ] **Step 2: 在反序列化中恢复 messageId**

修改 `deserialize()` 方法（第 112 行），在创建 Message 对象后（第 176 行 `message.setStoreSize(totalSize)` 之前），从 properties 中提取 messageId：

```java
// 从 properties 中提取 messageId
if (message.getProperties() != null && message.getProperties().containsKey("_MSG_ID_")) {
    String msgId = message.getProperties().get("_MSG_ID_");
    message.setMessageId(msgId);
    // 从业务 properties 中移除内部 key，对上层透明
    message.getProperties().remove("_MSG_ID_");
}
```

- [ ] **Step 3: 编译验证**

Run: `mvn compile -pl flare-mq-store -am`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add flare-mq-store/src/main/java/com/flare/mq/store/MessageSerializer.java
git commit -m "feat: serialize/deserialize messageId via properties in MessageSerializer"
```

---

### Task 3: BrokerRequestHandler — messageId 透传（Send + Pull）

**Files:**
- Modify: `flare-mq-broker/src/main/java/com/flare/mq/broker/BrokerRequestHandler.java`

**Interfaces:**
- Consumes: `Message.getMessageId()` (from Task 1)
- Produces: `SimpleMessage.messageId` field, populated pull responses

- [ ] **Step 1: 在 handleSendMessage 设置 messageId**

在 `handleSendMessage()` 第 107 行后添加（`storeMsg.setQueueId(queueId)` 之后）：

```java
storeMsg.setMessageId(sendReq.messageId);
```

- [ ] **Step 2: SimpleMessage 添加 messageId 字段**

修改 `SimpleMessage` 静态类（第 306 行）：

```java
static class SimpleMessage { public String messageId; public String topic; public String tags; public String body; }
```

- [ ] **Step 3: handlePullMessage 传递 messageId**

修改 `handlePullMessage()` 中构建 SimpleMessage 的逻辑（第 151-155 行）：

```java
SimpleMessage sm = new SimpleMessage();
sm.messageId = m.getMessageId();
sm.topic = m.getTopic();
sm.tags = m.getTags();
sm.body = m.getBody() != null ? new String(m.getBody(), StandardCharsets.UTF_8) : "";
msgs.add(sm);
```

- [ ] **Step 4: 编译验证**

Run: `mvn compile -pl flare-mq-broker -am`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add flare-mq-broker/src/main/java/com/flare/mq/broker/BrokerRequestHandler.java
git commit -m "feat: pass messageId through BrokerRequestHandler send and pull"
```

---

### Task 4: ConsumerImpl — messageId 从 Pull 响应解析

**Files:**
- Modify: `flare-mq-client/src/main/java/com/flare/mq/client/consumer/ConsumerImpl.java`

**Interfaces:**
- Consumes: `SimpleMessageDTO.messageId` (from Task 3 pull response)
- Produces: `com.flare.mq.client.producer.Message.messageId` populated after pull

- [ ] **Step 1: SimpleMessageDTO 添加 messageId 字段**

修改 `SimpleMessageDTO` 静态类（第 973 行）：

```java
static class SimpleMessageDTO {
    public String messageId;
    public String topic;
    public String tags;
    public String body;
}
```

- [ ] **Step 2: handlePullResponse 解析 messageId**

修改 `handlePullResponse()` 方法（第 847-851 行），在创建 Message 对象后设置 messageId：

```java
com.flare.mq.client.producer.Message msg =
        new com.flare.mq.client.producer.Message(
                sm.topic, sm.tags, sm.body.getBytes(StandardCharsets.UTF_8));
msg.setMessageId(sm.messageId);
messages.add(msg);
```

- [ ] **Step 3: 编译验证**

Run: `mvn compile -pl flare-mq-client -am`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add flare-mq-client/src/main/java/com/flare/mq/client/consumer/ConsumerImpl.java
git commit -m "feat: parse messageId from pull response in ConsumerImpl"
```

---

### Task 5: AckRecord + AckManager — 新增 storeSize + 更新 addPendingAck

**Files:**
- Modify: `flare-mq-broker/src/main/java/com/flare/mq/broker/ack/AckRecord.java`
- Modify: `flare-mq-broker/src/main/java/com/flare/mq/broker/ack/AckManager.java`

**Interfaces:**
- Consumes: (none — standalone)
- Produces: `AckRecord.storeSize`, `AckManager.addPendingAck()` new signature

- [ ] **Step 1: AckRecord 添加 storeSize 字段**

在 `messageOffset` 字段（第 60 行）后添加：

```java
/**
 * 消息在CommitLog中的存储大小
 */
private long storeSize;
```

添加 getter/setter：

```java
public long getStoreSize() {
    return storeSize;
}

public void setStoreSize(long storeSize) {
    this.storeSize = storeSize;
    this.lastUpdateTime = System.currentTimeMillis();
}
```

- [ ] **Step 2: AckManager.addPendingAck 增加 storeSize 参数**

修改 `addPendingAck()` 方法签名（第 107 行）：

```java
public void addPendingAck(String messageId, String consumerGroup, String topic, int queueId,
                          String consumerId, long messageOffset, long storeSize) {
    AckRecord record = new AckRecord(messageId, consumerGroup, topic, queueId, consumerId, messageOffset);
    record.setStoreSize(storeSize);
    ackRecords.put(messageId, record);
    logger.debug("添加待确认消息: messageId={}, consumerGroup={}", messageId, consumerGroup);
}
```

- [ ] **Step 3: 编译验证**

Run: `mvn compile -pl flare-mq-broker -am`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add flare-mq-broker/src/main/java/com/flare/mq/broker/ack/AckRecord.java \
        flare-mq-broker/src/main/java/com/flare/mq/broker/ack/AckManager.java
git commit -m "feat: add storeSize to AckRecord and update addPendingAck signature"
```

---

### Task 6: AckManager — RetryMessageHandler 回调 + checkRetryMessages

**Files:**
- Modify: `flare-mq-broker/src/main/java/com/flare/mq/broker/ack/AckManager.java`

**Interfaces:**
- Consumes: `AckRecord.getStoreSize()` (from Task 5)
- Produces: `RetryMessageHandler` interface, `setRetryHandler()` method

- [ ] **Step 1: 添加 RetryMessageHandler 接口**

在 `AckManager.java` 文件开头（`AckManager` 类声明之前）添加接口：

```java
package com.flare.mq.broker.ack;

// ... existing imports ...

/**
 * 重试消息处理器 — 由 Broker 层注入，负责消息的重新投递
 */
public interface RetryMessageHandler {
    void onRetryMessage(RetryRecord retryRecord, AckRecord ackRecord);
}
```

- [ ] **Step 2: 在 AckManager 中增加 retryHandler 字段和 setter**

在 `AckManager` 类字段区域（第 46 行后）添加：

```java
/**
 * 重试消息处理器
 */
private RetryMessageHandler retryHandler;

public void setRetryHandler(RetryMessageHandler retryHandler) {
    this.retryHandler = retryHandler;
}
```

- [ ] **Step 3: 改造 checkRetryMessages 使用回调**

修改 `checkRetryMessages()` 方法（第 316 行），将日志替换为回调：

```java
private void checkRetryMessages() {
    try {
        List<RetryRecord> retryMessages = getRetryMessages();

        for (RetryRecord record : retryMessages) {
            AckRecord ackRecord = ackRecords.get(record.getMessageId());
            if (record.getRetryCount() > DEFAULT_MAX_RETRY_TIMES) {
                moveToDeadLetterQueue(record.getMessageId(), "超过最大重试次数");
            } else {
                if (retryHandler != null) {
                    retryHandler.onRetryMessage(record, ackRecord);
                } else {
                    logger.warn("RetryMessageHandler not set, skipping retry for messageId={}",
                                record.getMessageId());
                }
            }
        }

    } catch (Exception e) {
        logger.error("检查重试消息失败", e);
    }
}
```

- [ ] **Step 4: 编译验证**

Run: `mvn compile -pl flare-mq-broker -am`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add flare-mq-broker/src/main/java/com/flare/mq/broker/ack/AckManager.java
git commit -m "feat: add RetryMessageHandler callback to AckManager for retry re-delivery"
```

---

### Task 7: BrokerRequestHandler — handleAckMessage 真正实现 + Pull PendingAck

**Files:**
- Modify: `flare-mq-broker/src/main/java/com/flare/mq/broker/BrokerRequestHandler.java`

**Interfaces:**
- Consumes: `AckManager.ackMessage()`, `AckManager.addToRetryQueue()`, `AckManager.addPendingAck()` (from Tasks 5-6)
- Produces: working ACK/retry flow on broker side

- [ ] **Step 1: BrokerRequestHandler 增加 AckManager 字段和构造函数参数**

修改构造函数（第 39 行）和字段区域：

```java
// 添加字段
private final AckManager ackManager;

// 扩展构造函数
public BrokerRequestHandler(TopicManager topicManager,
                            QueueManager queueManager,
                            DefaultMessageStore messageStore,
                            ConsumerOffsetManager offsetManager,
                            AckManager ackManager) {
    this.topicManager = topicManager;
    this.queueManager = queueManager;
    this.messageStore = messageStore;
    this.offsetManager = offsetManager;
    this.ackManager = ackManager;
}
```

- [ ] **Step 2: 添加 AckBody 内部类用于解析 ACK 请求**

在文件末尾的静态类区域（第 310 行之前）添加：

```java
static class AckBody { public String ackType; public String messageId; public String consumerGroup; public String topic; public int queueId; public String failureReason; }
```

- [ ] **Step 3: 实现 handleAckMessage**

替换 `handleAckMessage()` 方法（第 171 行）：

```java
private ProtocolMessage handleAckMessage(ProtocolMessage request) {
    String json = new String(request.getBody(), StandardCharsets.UTF_8);
    AckBody body = JsonUtils.fromJson(json, AckBody.class);
    if (body == null || body.messageId == null) {
        return ProtocolMessage.createErrorResponse(
                MessageType.ACK_MESSAGE_RESPONSE, request.getRequestId(), ResponseCode.BAD_REQUEST);
    }

    if ("FAILURE".equals(body.ackType)) {
        ackManager.addToRetryQueue(body.messageId, body.failureReason);
    } else {
        ackManager.ackMessage(body.messageId, body.consumerGroup);
    }

    return ProtocolMessage.createSuccessResponse(
            MessageType.ACK_MESSAGE_RESPONSE,
            request.getRequestId(),
            "OK".getBytes(StandardCharsets.UTF_8));
}
```

- [ ] **Step 4: handlePullMessage 注册 Pending Ack**

在 `handlePullMessage()` 构建 `PullResponse` 之前（第 158 行之后，`pr.maxOffset` 设置之后），添加 Pending Ack 注册：

```java
// 注册 Pending Ack
for (Message m : res.getMessageList()) {
    ackManager.addPendingAck(m.getMessageId(), pullReq.consumerGroup,
            pullReq.topic, pullReq.queueId,
            null, m.getCommitLogOffset(), m.getStoreSize());
}
```

- [ ] **Step 5: 编译验证**

Run: `mvn compile -pl flare-mq-broker -am`
Expected: BUILD SUCCESS

- [ ] **Step 6: Commit**

```bash
git add flare-mq-broker/src/main/java/com/flare/mq/broker/BrokerRequestHandler.java
git commit -m "feat: implement handleAckMessage with AckManager and register PendingAck on pull"
```

---

### Task 8: ConsumerImpl — consumeMessages 失败回传 + sendMessageBackToBroker

**Files:**
- Modify: `flare-mq-client/src/main/java/com/flare/mq/client/consumer/ConsumerImpl.java`

**Interfaces:**
- Consumes: `Message.getMessageId()` (from Task 4)
- Produces: failure messages sent back to broker via ACK request

- [ ] **Step 1: 新增 sendMessageBackToBroker 方法**

在 `ConsumerImpl` 类中（`reportOffsetToBroker` 方法附近）添加：

```java
/**
 * Send message back to broker for retry when consume fails.
 */
private void sendMessageBackToBroker(Message msg, String reason) {
    try {
        TopicRouteInfo routeInfo = getTopicRouteInfo(msg.getTopic());
        if (routeInfo == null) {
            logger.warn("No route info for topic {}, skip send back", msg.getTopic());
            return;
        }

        TopicRouteInfo.BrokerInfo brokerInfo = routeInfo.getBrokerInfos().get(0);
        NettyClient brokerClient = getBrokerClient(brokerInfo.getBrokerName(), routeInfo);
        if (brokerClient == null) {
            logger.warn("No broker client for {}, skip send back", brokerInfo.getBrokerName());
            return;
        }

        String json = String.format(
            "{\"ackType\":\"FAILURE\",\"messageId\":\"%s\",\"consumerGroup\":\"%s\",\"topic\":\"%s\",\"queueId\":%d,\"failureReason\":\"%s\"}",
            msg.getMessageId(), config.getConsumerGroup(),
            msg.getTopic(), msg.getQueueId(),
            reason != null ? reason : "unknown");
        ProtocolMessage request = new ProtocolMessage(
                MessageType.ACK_MESSAGE_REQUEST, json.getBytes(StandardCharsets.UTF_8));
        brokerClient.sendSync(request, config.getPullMsgTimeout());
        logger.debug("Sent message back for retry: messageId={}", msg.getMessageId());
    } catch (Exception e) {
        logger.error("Failed to send message back to broker: messageId=" + msg.getMessageId(), e);
    }
}
```

- [ ] **Step 2: 修改 consumeMessages 失败分支**

修改 `consumeMessages()` 方法（第 680 行），在失败分支调用 `sendMessageBackToBroker` 替代只打日志，并移除 break 保持继续处理后续消息：

```java
if (status.isSuccess()) {
    stats.recordConsumeSuccess(costTime, message.getMessageSize());
    ackMessage(message.getMessageId());
    // Report offset to broker
    String topic = message.getTopic();
    String progressKey = topic + "_" + message.getQueueId();
    long currentOffset = consumeProgress.getOrDefault(progressKey, 0L);
    reportOffsetToBroker(topic, message.getQueueId(), currentOffset);
} else {
    stats.recordConsumeFailure(costTime);
    logger.warn("Consume message failed, sending back for retry: messageId={}", message.getMessageId());
    sendMessageBackToBroker(message, status.getReason());
    // 不 break，继续处理同批后续消息
}
```

- [ ] **Step 3: 更新 buildAckRequest 兼容 SUCCESS ACK**

修改 `buildAckRequest()` 方法（第 866 行），添加 ackType 字段：

```java
private ProtocolMessage buildAckRequest(List<String> messageIds) {
    StringBuilder sb = new StringBuilder();
    sb.append("{\"ackType\":\"SUCCESS\",\"messageIds\":[");
    for (int i = 0; i < messageIds.size(); i++) {
        if (i > 0) sb.append(",");
        sb.append("\"").append(messageIds.get(i)).append("\"");
    }
    sb.append("],\"consumerGroup\":\"").append(config.getConsumerGroup()).append("\"}");

    return new ProtocolMessage(MessageType.ACK_MESSAGE_REQUEST, sb.toString().getBytes());
}
```

- [ ] **Step 4: 编译验证**

Run: `mvn compile -pl flare-mq-client -am`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add flare-mq-client/src/main/java/com/flare/mq/client/consumer/ConsumerImpl.java
git commit -m "feat: send failed messages back to broker for retry in consumeMessages"
```

---

### Task 9: ClusterManager — 初始化 AckManager 并注入 RetryMessageHandler

**Files:**
- Modify: `flare-mq-broker/src/main/java/com/flare/mq/broker/cluster/ClusterManager.java`

**Interfaces:**
- Consumes: `BrokerRequestHandler` new constructor (from Task 7), `AckManager` (from Tasks 5-6)
- Produces: fully wired ACK/retry pipeline

- [ ] **Step 1: 添加 AckManager 字段和 import**

在 `ClusterManager` 文件中添加 import：

```java
import com.flare.mq.broker.ack.AckManager;
import com.flare.mq.broker.ack.RetryMessageHandler;
```

添加字段（在 `ConsumerOffsetManager` 字段之后）：

```java
// ACK/重试管理器
private AckManager ackManager;
```

- [ ] **Step 2: 在 start() 方法中初始化 AckManager 并构造 BrokerRequestHandler**

在 `start()` 方法中，`BrokerRequestHandler` 构造之前，初始化 AckManager：

```java
// 初始化 AckManager
this.ackManager = new AckManager();
this.ackManager.start();

// 设置重试消息处理器
this.ackManager.setRetryHandler((retryRecord, ackRecord) -> {
    try {
        com.flare.mq.store.Message message = messageStore.getMessage(
                ackRecord.getMessageOffset(), (int) ackRecord.getStoreSize());
        if (message != null) {
            messageStore.putMessage(message);
            logger.info("Retry message re-delivered: messageId={}, retryCount={}",
                    retryRecord.getMessageId(), retryRecord.getRetryCount());
        } else {
            logger.warn("Retry message not found in CommitLog: messageId={}, offset={}",
                    retryRecord.getMessageId(), ackRecord.getMessageOffset());
        }
    } catch (Exception e) {
        logger.error("Retry re-delivery failed: messageId="
                + retryRecord.getMessageId(), e);
    }
});
```

修改 `BrokerRequestHandler` 构造调用，增加 `ackManager` 参数（第 94 行）：

```java
new BrokerRequestHandler(topicManager, queueManager, messageStore, offsetManager, ackManager));
```

- [ ] **Step 3: 编译验证**

Run: `mvn compile -pl flare-mq-broker -am`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add flare-mq-broker/src/main/java/com/flare/mq/broker/cluster/ClusterManager.java
git commit -m "feat: initialize AckManager in ClusterManager with RetryMessageHandler"
```

---

### Task 10: 运行所有测试 + 最终验证

- [ ] **Step 1: 运行全量测试**

Run: `mvn test`
Expected: 全部 PASS（AckManagerTest 需适配 addPendingAck 签名变更）

- [ ] **Step 2: 检查 AckManagerTest 是否需要适配**

Run: `mvn test -pl flare-mq-broker -Dtest=AckManagerTest`
Expected: PASS，如有签名不匹配修复测试

- [ ] **Step 3: 如有测试失败，修复后重新运行**

```bash
mvn test
```

- [ ] **Step 4: 最终 commit（如有测试修复）**

```bash
git add -A
git commit -m "test: adapt tests for AckManager API changes"
```
